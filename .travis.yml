language: node_js
env:
  - NODE_VERSION=12 CC=clang CXX=clang++
before_install:
  - nvm install $NODE_VERSION
branches:
  only:
    - master
addons:
  chrome: stable
script:
  - ng test --watch=false
after_success:
  - ng deploy --base-href=https://dECRISES.github.io/app-generator/ --message="docs auto generated commit"